package com.example
//_________________________________________________________
//Basic types:
//Numbers → Byte, Short, Int, Long, Float, Double
//Characters → Char
//Boolean → Boolean
//Strings → String
//--------------------------------------------------------
//Declare two integers (Int).
//Find and print their sum, difference, product, and quotient.
//Also declare one of them as Double and see what happens when dividing (observe the result difference).

fun  main(){
    val a : Int = 4
    val b : Double = 2.0
    println("Sum : ${a + b}")
    println("Difference : ${a - b}")
    println("Product : ${a * b}")
    println("Quotient : ${a / b}")
}